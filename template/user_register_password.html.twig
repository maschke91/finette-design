{% extends 'layout/no_menu.html.twig' %}

{% import 'macro/form.html.twig' as form %}

{% block content %}
<section class="layout__box-wrap">
  <div class="layout__box-content">
    <div class="layout__box-img">
      <img src="/assets/img/logo{{ subdom }}.svg" alt="Finettte s.r.o." />
    </div>
    <h2 class="layout__box-title">Registrace</h2>
    <form action="/user/ajax_register_password" data-name="user_register_password" novalidate>
      {{ form.input('Heslo', 'password', [{
        'type' : 'empty',
        'text' : 'Musíte vyplnit heslo'
      }], null, 'password', false, 'password') }}
      <br />
      {{ form.input('Heslo znovu', 'password_check', [{
        'type' : 'empty',
        'text' : 'Musíte vyplnit heslo znovu'
      }, {
        'type' : 'valid',
        'text' : 'Nová hesla se neshodují'
      }], null, 'password', false, 'password') }}
      <div class="form__button-wrap button-wrap-vertical">
        {{ form.hidden('token', token) }}
        {{ form.submit('Pokračovat', 'Pokračuji...') }}
      </div>
    </form>
  </div>
</section>
{% endblock %}
